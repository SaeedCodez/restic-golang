# Compiled binary
/restic-web

# Local app state
/config.json

# Default local repository directory
/restic-repo/

# New app state directory
/data/

# Front-end toolchain. `ui/` holds the UI source; its build output is committed
# to `web/` (which main.go embeds), so `go build` never needs Node.
/ui/node_modules/
/ui/dist/
